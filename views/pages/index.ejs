<!-- views/pages/index.ejs -->
<!DOCTYPE html>
<html lang="en">
  <% include ../partials/head %>
  <body>
    <% include ../partials/nav %>
    <div class="container">
      <h2>Home Page</h2>
      <ul class="list-group">
        <% pets.forEach(pet => { %>
          <li class="list-group-item">Pet Id:
            <%= pet.id%> Name:
              <%= pet.name%> Age:
                <%= pet.age%>
          </li>
        <%})%>
        </ul>      
    </div>
    <% include ../partials/footer %>
    <% include ../partials/scripts %>
  </body>

</html>